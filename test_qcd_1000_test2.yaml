event:
  embed_dim: 64
  mode: mlp
  hidden_sizes: (32, 32)
  nonlinearity: relu
  nested_fields:
    embed_dim: 64
    mode: mlp
    hidden_sizes: (32, 32)
    nonlinearity: relu
    fields:
    - Event:
        embed_dim: 64
        mode: mlp
        hidden_sizes: (32, 32)
        nonlinearity: relu
        jagged_fields:
          embed_dim: 64
          mode: lstm
          fields:
          - Event.fUniqueID
          - Event.fBits
          - Event.Number
          - Event.ReadTime
          - Event.ProcTime
          - Event.ProcessID
          - Event.MPI
          - Event.Weight
          - Event.Scale
          - Event.AlphaQED
          - Event.AlphaQCD
          - Event.ID1
          - Event.ID2
          - Event.X1
          - Event.X2
          - Event.ScalePDF
          - Event.PDF1
          - Event.PDF2